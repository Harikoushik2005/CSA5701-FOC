#include <stdio.h>

int main() {
    float num, square;

    printf("Enter a decimal number: ");
    scanf("%f", &num);

    square = num * num;

    printf("Square of %.2f is %.2f\n", num, square);

    return 0;
}
